source "https://rubygems.org"

# Local dev/verification only. The GitHub Pages deploy (see
# .github/workflows/jekyll-gh-pages.yml) builds with the `github-pages` gem
# via actions/jekyll-build-pages. This simple site (one page, custom layout,
# no plugins) renders identically on Jekyll 4, which supports modern Ruby.
gem "jekyll", "~> 4.4"

# Ruby 3.4+ no longer ships these in the stdlib.
gem "csv", "~> 3.3"
gem "bigdecimal"
gem "webrick"
